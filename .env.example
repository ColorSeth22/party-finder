# Environment Variables for PartyFinder

# PostgreSQL Database Connection
# Format: postgres://username:password@host:port/database
# Local development example (local postgres):
DATABASE_URL=postgres://postgres:postgres@localhost:5432/party_finder

# For Vercel deployment, set this in your Vercel project settings.
# Example with Vercel Postgres (replace placeholders):
# DATABASE_URL=postgres://default:password@abcdefg-postgres.vercel-storage.com:5432/verceldb

# Node environment (automatically set by Vercel)
NODE_ENV=development

# JWT Secret for authentication tokens (use a strong random string in production)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=change_me_dev_secret

# Optionally add other future variables here (e.g. S3 bucket for media)
# MEDIA_BASE_URL=http://localhost:3000/uploads
